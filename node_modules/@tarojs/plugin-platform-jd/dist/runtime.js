import { processApis, mergeReconciler } from '@tarojs/shared';

function initNativeApi(taro) {
    processApis(taro, jd);
}

const hostConfig = {
    initNativeApi
};

mergeReconciler(hostConfig);
//# sourceMappingURL=runtime.js.map
