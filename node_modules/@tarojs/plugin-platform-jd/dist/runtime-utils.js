import { processApis } from '@tarojs/shared';

function initNativeApi(taro) {
    processApis(taro, jd);
}

const hostConfig = {
    initNativeApi
};

export { hostConfig, initNativeApi };
//# sourceMappingURL=runtime-utils.js.map
